{
  "layout": "layouts/job.njk",
  "tags": ["jobs"],
  "permalink": "/{{ page.fileSlug }}/",
  "eleventyComputed": {
    "title": "{{ title }}",
    "description": "{{ title }} — {{ city }}, {{ workingType }} — {{ salary }}"
  }
}
