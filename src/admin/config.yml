backend:
  name: github
  repo: craciunpv-arch/job-postings
  branch: main
  base_url: https://job-postings-nu.vercel.app
  auth_endpoint: api/auth

media_folder: "src/images/uploads"
public_folder: "/images/uploads"

collections:
  - name: jobs
    label: "Job Postings"
    folder: src/jobs
    create: true
    slug: "{{slug}}"
    fields:
      - { label: "Role Title", name: title, widget: string }
      - { label: "Monthly Net Salary", name: salary, widget: string }
      - { label: City, name: city, widget: string }
      - label: "Working Type"
        name: workingType
        widget: select
        options: ["Remote", "Hybrid", "Onsite"]
      - label: Status
        name: status
        widget: select
        options: ["Open", "Closed"]
        default: "Open"
      - { label: "Publish Date", name: date, widget: datetime }
      - { label: "Client Description", name: clientDescription, widget: markdown }
      - { label: "Main Responsibilities", name: responsibilities, widget: list }
      - { label: "Must-haves", name: mustHaves, widget: list }
      - { label: "Nice-to-haves", name: niceToHaves, widget: list }
      - { label: "Other Benefits", name: benefits, widget: list }
      - { label: "Selection Process", name: selectionProcess, widget: list }
      - { label: "Good to Know", name: goodToKnow, widget: list }

  - name: settings
    label: "Site Settings"
    files:
      - file: src/_data/site.json
        name: site
        label: "Author & Site Info"
        fields:
          - { label: Name, name: name, widget: string }
          - { label: "LinkedIn URL", name: linkedin, widget: string }
          - { label: "Contact Note", name: contactNote, widget: text }
